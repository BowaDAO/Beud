import React from "react";
import { NavigationBar, Hero } from "../components";

const Header = () => {
  return (
    <header>
      <NavigationBar />
      <Hero />
    </header>
  );
};

export default Header;
