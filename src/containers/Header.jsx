import React from "react";
import { NavigationBar, Hero } from "../components";

const Header = () => {
  return (
    <section>
      <NavigationBar />
      <Hero />
    </section>
  );
};

export default Header;
